version: 2.1
jobs:
  hello-world:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Print Hello World
          command: echo "Hello, World!"
workflows:
  version: 2
  hello-world-workflow:
    jobs:
      - hello-world:
          filters:
            branches:
              only: main
